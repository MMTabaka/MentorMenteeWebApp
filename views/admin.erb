<%= erb :"common/header" %>
  <link rel="stylesheet" href="css/mentorList.css">
  <title>Dashboard</title>
</header>

<body>
<div class="wrapper">
<div class="bg-warning">
  <div class="navi">
    <ul class="nav justify-content-end nav-pills">
      <li>
        <a class="nav-link text-body"><%= @admin_name %></a>
      </li>
      <li>  
        <a class="nav-link text-body active" href="logout">Logout</a>
      </li>
    </ul>
  </div>
</div>
    
<div class="container">
  <div class="content">
    <div class="table-responsive">
      <table class="table table-hover table-borderless align-middle">
        <thead>
          <tr>
            <th scope="col">ID</th>
            <th scope="col">Name</th>
            <th scope="col">Email</th>
            <th scope="col">Type</th>
            <th scope="col">Suspension</th>
            <th scope="col"></th>
          </tr>
        </thead>
        <tbody>
          <% (@users).each do |i|%>
            <tr>
              <th id="<%= i[:id] %>" scope="row"><%= i[:id] %></th>
              <td id="<%= i[:id] %>Username"><%= i[:name] %></td>
              <td id="<%= i[:id] %>Email"><%= i[:email] %></td>
              <td id="<%= i[:id] %>Type"><%= i[:user_type] %></td>
              <td id="<%= i[:id] %>Suspension"><%= i[:suspension] %></td>
              <td>
                <a href="/profile/<%= i[:id] %>" id="<%= i[:id] %>Profile Details" type="submit" class="btn btn-primary btn-lg btnC">Detail</a>
                <button id="<%= i[:id] %>-reset-pass" data-id="<%= i[:id] %>" type="submit" class="btn btn-primary btn-lg btnC reset-btn">Reset Password</button>
                <button id="<%= i[:id] %>-suspend" data-id="<%= i[:id] %>" class="btn btn-primary btn-lg btnC suspend-btn">
                  <% if i[:suspension] == 0 %>
                    Suspend
                  <% else %>
                    Unsuspend
                  <% end %>
                </button>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
  <script src="js/admin.js"></script>
<%= erb :"common/footer" %>